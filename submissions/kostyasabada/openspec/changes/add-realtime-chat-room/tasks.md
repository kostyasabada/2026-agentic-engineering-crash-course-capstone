# Tasks

Rules for every task below (from `docs/review-process.md`): the main agent only coordinates. Each task gets a fresh maker subagent with a scoped handoff (task ID `add-realtime-chat-room-<group>-<n>`, e.g. `add-realtime-chat-room-2-1`, acceptance criteria, spec/design paths, owned files, required checks) and no full history; a separate checker (a Claude Code subagent) reviews the frozen snapshot and reruns the checks. Evidence goes to `docs/evidence/<task-id>/` (`implementation.md`, `checks.txt` with actual commands, outputs, exit codes, date, and `node --version`, `snapshot.txt`, `review.md`). Findings return to the same maker. A checkbox is ticked only after the checker accepts the final snapshot. Tests are written red-first where practical: record the failing run before the implementation makes it pass. An optional extra review by Codex/ChatGPT may be added to a task's evidence only when it was actually performed; it is not a completion gate.

## 0. Decisions gate

- [ ] 0.1 Confirm that the user decisions of 2026-09-26 (Q1–Q7, Q9, and the principles "restrict the Socket.IO Origin" and "simplify the loop: 3–4 stop reasons, no `next build` per iteration") are recorded in `docs/evidence/README.md`, `docs/architecture.md`, and `design.md`, and obtain the user's answer on the remaining design.md proposals P1–P22 and open question Q11 (P19 gates the Host/Origin requirement's mechanism and P20 gates the chat-room spec's no-truncation rule; if either is changed, update the spec) (SQLite driver); record the answers in the user's words in `docs/evidence/add-realtime-chat-room-0-1/decisions.md`; if any answer changes the approach, update proposal/specs/design/tasks with `openspec-update-change`. Verify: `npm run --silent openspec -- validate add-realtime-chat-room --strict` exits 0 and the checker confirms every recorded decision matches the user's own words.

## 1. Tooling, check commands, and agent tooling

- [ ] 1.1 Replace the pinned `typescript` 7.0.2 with 6.0.3 and `eslint` 10.11.0 with 9.39.5 (user decision, 2026-09-26); install the confirmed dependencies with exact versions (Next.js, React, React DOM, Socket.IO server and client, zod, SQLite driver, tsx, `eslint-config-next`, `@types/node`, `@types/react`, `@types/react-dom`, driver types if needed); add `.gitignore` entries (`.next/`, `data/`, `test-results/`, `playwright-report/`, `.agent-loop/`, `next-env.d.ts` per Next docs). Verify: `npx tsc --version` prints 6.0.3 and `npx eslint --version` prints v9.39.5; `npm ci` from the lock file exits 0; `npm ls` reports no missing or invalid peers (in particular for `typescript-eslint` and the `eslint-plugin-*` packages); versions in `package.json` match the decisions.
- [ ] 1.2 Add the npm scripts `dev`, `build`, `start`, `lint`, `typecheck`, `test:unit`, `test:e2e`, and `test:e2e:dev` exactly as in design D5; add `tsconfig.json` (strict), `eslint.config.mjs` (including `react/no-danger`), `vitest.config.ts`, `playwright.config.ts` (design D5–D6), the minimal App Router shell (`src/app/layout.tsx`, `src/app/page.tsx` with the page title), `src/server/config.ts` with red-first unit tests for env parsing (defaults, invalid `PORT` rejected), `server.ts` serving Next.js and attaching Socket.IO (`destroyUpgrade: false`), the E2E server fixture `e2e/fixtures/chat-server.ts` supporting `E2E_SERVER_MODE=prod|dev`, and one E2E smoke test that the page loads through the custom server. Verify: `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:e2e`, and `npm run test:e2e:dev` each run real checks and exit 0; the red run of the config tests is recorded.
- [ ] 1.3 Add the `check` and `check:loop` scripts exactly as in design D5 and confirm they fail fast: introduce a lint error in a scratch branch or temporary copy, record the non-zero exit of both, then record green runs with their durations; confirm that running `test:e2e:dev` after `test:e2e` (and the reverse) does not break either run's build output, using a separate `distDir` if needed. Verify: `npm run check` and `npm run check:loop` exit 0 on the final snapshot, and the recorded non-zero runs.
- [ ] 1.4 Implement `scripts/agent-loop.sh` per `specs/agent-loop/spec.md` and design D7, with `scripts/agent-loop.test.ts` (Vitest) driving it through `AGENT_LOOP_CHECK_CMD`, `AGENT_LOOP_FULL_CHECK_CMD`, and fake `claude`/`codex` executables on `PATH`, covering: usage errors (64) for missing task id, invalid iteration limits, and unknown agent; invocation from another directory; default agent `claude` and limit 5 in the header; already green; fixed within the limit; full check failing after the fast check passes; limit reached (1); `agent_error` (2) for missing executable, non-zero exit, and timeout; `no_progress` (3); no `no_progress` when the fake fixer edits only an untracked, non-ignored file (in a temporary Git repository); a broken untracked symlink and an unreadable untracked file do not abort the loop, and retargeting the symlink counts as progress; a fixer run that changes nothing while `loop-run.log` is already committed and gets appended still stops with `no_progress`; interrupt during a long-running fake fixer terminates it and its children (its PID no longer exists) and exits 130; log header/entries/footer; append-only log; and no checkbox/commit changes. Confirm the real CLI flags with `claude --help` and record the output; if Codex is installed, also record `codex exec --help`, otherwise record that it is not installed. Verify: `npm run test:unit` exit 0 with these tests, `npm run check` exit 0, and a manual `scripts/agent-loop.sh` usage-error run recorded.
- [ ] 1.5 Implement `scripts/session-context.sh` and `.claude/settings.json` per `specs/session-context-hook/spec.md` and design D8; evaluate Codex parity against the installed Codex version's documentation or `--help` (or record that Codex is not installed) and document the result or the limitation in `docs/workflow.md`; update the "neither exists yet" note there. Verify: run the script with `CLAUDE_PROJECT_DIR` set (success), with OpenSpec unavailable (e.g. `PATH` without `node`) and with a forced timeout (notice, exit 0), and `git status --short` unchanged by the script; record the actual output of the hook in a real new Claude Code session (for example with `claude --debug` hook logs) in `docs/evidence/add-realtime-chat-room-1-5/hook-output.txt`. State plainly anything that could not be observed.
- [ ] 1.6 Update `docs/architecture.md` (record the confirmed proposals from task 0.1, keep limitations), `docs/testing.md` (exact commands: `npm run check`, `npm run check:loop`, `npm run test:unit`, `npm run test:e2e`, `npm run test:e2e:dev`, Chromium prerequisite), and `README.md` (run instructions: `npm run dev`, `npm run build`, `npm start`) without copying requirements. Verify: each documented command was run as written and its result recorded; `git diff --check` clean.

## 2. Shared message schema (real task through the agent loop)

- [ ] 2.1 Maker writes red-first unit tests in `src/lib/chat/schema.test.ts` for the accepted nickname and message rules: valid, empty, whitespace-only, too long, disallowed characters, exact limits, trimming, preserved inner line breaks, UTF-16 code-unit counting with astral characters (16 vs. 17 copies of U+20000 as a nickname; a 1000-unit message containing astral characters), and stripping of extra fields; records the failing `npm run check:loop`; writes `docs/evidence/add-realtime-chat-room-2-1/loop-brief.md`; and launches `scripts/agent-loop.sh --task-id add-realtime-chat-room-2-1 --brief docs/evidence/add-realtime-chat-room-2-1/loop-brief.md` (default agent `claude`) so the fixer implements `src/lib/chat/schema.ts`; commit `docs/evidence/add-realtime-chat-room-2-1/loop-run.log` unedited. Verify: the log header shows `check_cmd="npm run check:loop"` and `full_check_cmd="npm run check"`, the footer shows the actual stop reason, `npm run check` exits 0 on the final snapshot, and the separate checker reviews the diff, the brief, and `loop-run.log` and reruns `npm run check` before acceptance. If the loop does not reach green, the maker finishes the work (optionally rerunning the loop) and the log records every run.

## 3. Persistence

- [ ] 3.1 Implement `src/server/chat/message-store.ts` with red-first tests in `message-store.test.ts` using a temporary DB file: ids increase and are never reused, server UTC timestamps are assigned, `latest(100)` after 105 inserts returns messages 6–105 oldest first, fewer than 100 returns all, `since(lastSeenId, 101)` returns only newer rows, data survives closing and reopening the file, schema creation is idempotent. Verify: `npm run test:unit` red run recorded, then `npm run check` exit 0.

## 4. Real-time server

- [ ] 4.1 Implement `src/server/chat/socket-handlers.ts` per design D2 with red-first Node integration tests in `socket-handlers.test.ts` (Socket.IO on an ephemeral port, temp DB, `socket.io-client`): history `replace` on first connect; broadcast to another independent client and to the sender; two clients sending in quick succession receive the messages in the same order matching server ids; rejection of empty/whitespace/oversized text and invalid nickname with an ack error to the sender only and nothing stored or broadcast; client ids/timestamps ignored; catch-up `append` with `lastSeenId`; `replace` with exactly messages 51–150 when `lastSeenId` is 10 and messages 11–150 were missed; unit tests for `src/server/host-policy.ts` (allowlist from `localhost`, `127.0.0.1`, `[::1]`, and a non-wildcard `HOST`; wildcard `0.0.0.0`/`::` adds nothing; normalization of uppercase hosts and of `PORT=80`; `null`, missing, and unparsable values). Verify: red run recorded, then `npm run check` exit 0.
- [ ] 4.2 Wire the handlers and message store into `server.ts` with the Host check for every request and upgrade and the Origin check in `allowRequest` (design D2, Proposal P19), plus red-first Node tests using raw HTTP requests against the running server: a handshake with no `Origin` and `Host: evil.example:<PORT>` refused on both the polling and the websocket transport; `GET /` with that `Host` refused with 403; with an allowed `Host`, `Origin: http://evil.example` (separate case), `Origin: null`, and an unparsable `Origin` refused without crashing the server; no `Origin` with `Host: 127.0.0.1:<PORT>`, `Host: LOCALHOST:<PORT>`, and `Origin: http://localhost:<PORT>` accepted; with `HOST=0.0.0.0`, `Host: 0.0.0.0:<PORT>` refused; graceful shutdown (close Socket.IO and the database on `SIGINT`/`SIGTERM`), and the default `data/chat.sqlite` path (directory created if missing). Verify: `npm run check` exit 0; manual `npm run build && npm start` then `curl -sI http://127.0.0.1:3000/` returns 200 and `curl -sI -H 'Host: evil.example:3000' http://127.0.0.1:3000/` returns 403, recorded.

## 5. Chat UI and end-to-end scenarios

- [ ] 5.1 Implement the nickname form (`src/app/_chat/nickname-form.tsx`, `chat-room.tsx`) with red-first E2E tests in `e2e/nickname.spec.ts`: valid trimmed nickname accepted; empty/whitespace, too long (full text kept, no truncation), and disallowed characters rejected with messages; remembered after reload; changed nickname applies to later messages only and survives reload; invalid change rejected keeping the previous nickname; duplicates allowed across two contexts. Verify: red run recorded, then `npm run check` exit 0.
- [ ] 5.2 Implement the message list, composer, and `use-chat-socket.ts` plus `merge-messages.ts` (unit tests for merge, dedupe, and ordering) with red-first E2E tests in `e2e/messaging.spec.ts`: delivery to another independent browser context without reload; both contexts show two quickly sent messages in the same order; markup rendered literally (no element created, no script run); a three-line message shown on three lines; trimming; empty and whitespace-only not sent; oversized typed or pasted text kept in full (no truncation) with the limit message and sending blocked; a counter of the trimmed length shown; exactly-at-limit accepted; 1000 characters with surrounding spaces (1020 raw) accepted; timestamp shown as `HH:MM` (browser context time zone set to UTC); oversized and invalid-nickname payloads from a modified `socket.io-client` rejected and not shown; a `socket.io-client` connection with a foreign `Origin` refused; send failure keeps the text. Verify: red run recorded, then `npm run check` exit 0.
- [ ] 5.3 Implement the connection status and reconnection behavior with red-first E2E tests in `e2e/connection.spec.ts`: stopping the fixture server shows `Reconnecting` or `Disconnected` and disables sending with typed text kept; restarting shows `Connected` without reload; missed messages are caught up in order without duplicates (via `page.routeWebSocket` or the documented fallback from design D6, with the limitation recorded if the fallback is used). Verify: red run recorded, then `npm run check` exit 0.
- [ ] 5.4 Add red-first E2E tests in `e2e/history.spec.ts`: seeded 105 messages show exactly 6–105 oldest first with message 105 visible without scrolling; a new message from another client scrolls into view when the person is at the bottom, and does not move the view when the person has scrolled up; 3 messages show all 3; empty room hint; history identical after server restart; implement any missing UI pieces. Verify: red run recorded (or, if already green because earlier tasks implemented the behavior, state that explicitly), then `npm run check` exit 0.

## 6. Integration

- [ ] 6.1 From a clean checkout of the final commit, run `npm ci`, `npm exec -- playwright install chromium` (if needed), `npm run check`, and `npm run --silent openspec -- validate --all --strict`; the checker reruns them independently. Supplementary: a manual two-browser session with screenshots, labelled as supplementary. Verify: all commands exit 0, outputs recorded in `docs/evidence/add-realtime-chat-room-6-1/checks.txt`, checker verdict `accepted` in `review.md`.
